<%= form_for @timetrackingtype, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> Timetrackingtype</legend>

    <div class="control-group">
      <%= f.label :Name, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :Name, :class => 'text_field' %>
      </div>
    </div>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', timetrackingtypes_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
