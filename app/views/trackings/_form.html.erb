<%= form_for @tracking, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> Tracking</legend>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', trackings_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
